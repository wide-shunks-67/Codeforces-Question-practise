#include <iostream>#include <string>#include <algorithm>#include <vector> using namespace std; int count_blocks(string s) {    int blocks = 1;     for (int i = 0; i < s.length() - 1; i++) {        if (s[i] != s[i+1]) {            blocks++;        }    }    return blocks;} int main() {    int t;    cin >> t;    while (t--) {        int n;        string s;        cin >> n >> s;         int max_blocks = 0;                       for (int i = 0; i < n; i++) {                        s = s.substr(1) + s[0];                         max_blocks = max(max_blocks, count_blocks(s));        }                cout << max_blocks << "\n";    }    return 0;}